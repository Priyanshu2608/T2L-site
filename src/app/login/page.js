import LoginClient from "@/components/LoginClient";

export const metadata = {
  title: "Log in · Turn2Law",
  description: "Log in to your Turn2Law account — India's Legal Operating System for founders and lawyers.",
};

export default function LoginPage() {
  return <LoginClient />;
}
